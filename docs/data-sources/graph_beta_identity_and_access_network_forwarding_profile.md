---
page_title: "microsoft365_graph_beta_identity_and_access_network_forwarding_profile Data Source - terraform-provider-microsoft365"
subcategory: "Identity and Access"
description: |-
  Retrieves Microsoft Entra Global Secure Access forwarding profiles using Microsoft Graph beta /networkAccess/forwardingProfiles and expands associated forwarding policy links.
---

# microsoft365_graph_beta_identity_and_access_network_forwarding_profile (Data Source)

Retrieves Microsoft Entra Global Secure Access forwarding profiles using Microsoft Graph beta `/networkAccess/forwardingProfiles` and expands associated forwarding policy links.

This data source reads Microsoft Entra Global Secure Access forwarding profiles. Microsoft creates the default forwarding profiles, including the Internet Access profile, and links Microsoft-managed forwarding policies to those profiles.

## Microsoft Documentation

- [What is Global Secure Access?](https://learn.microsoft.com/en-us/entra/global-secure-access/overview-what-is-global-secure-access)
- [Global Secure Access traffic forwarding profiles](https://learn.microsoft.com/en-us/entra/global-secure-access/concept-traffic-forwarding)
- [Internet Access](https://learn.microsoft.com/en-us/entra/global-secure-access/concept-internet-access)
- [forwardingProfile resource type](https://learn.microsoft.com/en-us/graph/api/resources/networkaccess-forwardingprofile?view=graph-rest-beta)

## Microsoft Graph API

This data source uses Microsoft Graph beta `/networkAccess/forwardingProfiles` and item reads with `$expand=policies($expand=policy)`.

## Microsoft Graph API Permissions

The following client `application` permissions are needed in order to use this data source:

**Required:**
- `NetworkAccess.Read.All`

**Optional:**
- `None` `[N/A]`

## Filtering

This data source supports filtering using the following attributes:

- `forwarding_profile_id` (Optional): The forwarding profile ID. Conflicts with `name`, `traffic_forwarding_type`, and `list_all`.
- `name` (Optional): The forwarding profile name to match exactly, ignoring case. Conflicts with `forwarding_profile_id`, `traffic_forwarding_type`, and `list_all`.
- `traffic_forwarding_type` (Optional): The forwarding profile traffic type. Valid values are `internet`, `m365`, and `private`. Conflicts with `forwarding_profile_id`, `name`, and `list_all`.
- `list_all` (Optional): Retrieve all forwarding profiles. Conflicts with `forwarding_profile_id`, `name`, and `traffic_forwarding_type`.

## Behavior Notes

- `policy_link_id` identifies the link between a forwarding profile and a policy.
- `policy_id` identifies the linked forwarding policy. Use this ID with Internet Access forwarding policy rule resources.
- Microsoft-managed forwarding profile policy links are returned for discovery and state management; they are not created by this provider.

## Example Usage

### Example 1: List all forwarding profiles

```terraform
data "microsoft365_graph_beta_identity_and_access_network_forwarding_profile" "all" {
  list_all = true

  timeouts = {
    read = "3m"
  }
}

output "internet_forwarding_profile" {
  value = [
    for profile in data.microsoft365_graph_beta_identity_and_access_network_forwarding_profile.all.items : {
      id                      = profile.id
      name                    = profile.name
      traffic_forwarding_type = profile.traffic_forwarding_type
      policies = [
        for link in profile.policies : {
          policy_link_id = link.policy_link_id
          policy_id      = link.policy_id
          policy_name    = link.policy_name
          state          = link.state
        }
      ]
    }
    if profile.traffic_forwarding_type == "internet"
  ]
}
```

### Example 2: Get profile by name

```terraform
data "microsoft365_graph_beta_identity_and_access_network_forwarding_profile" "internet" {
  name = "Internet traffic forwarding profile"
}
```

### Example 3: Get Internet Access profile

```terraform
data "microsoft365_graph_beta_identity_and_access_network_forwarding_profile" "internet" {
  traffic_forwarding_type = "internet"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `forwarding_profile_id` (String) The forwarding profile ID. Conflicts with `name`, `traffic_forwarding_type`, and `list_all`.
- `list_all` (Boolean) Retrieve all forwarding profiles. Conflicts with `forwarding_profile_id`, `name`, and `traffic_forwarding_type`.
- `name` (String) The forwarding profile name to match exactly, ignoring case. Conflicts with `forwarding_profile_id`, `traffic_forwarding_type`, and `list_all`.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))
- `traffic_forwarding_type` (String) The forwarding profile traffic type. Valid values are `internet`, `m365`, and `private`. Conflicts with `forwarding_profile_id`, `name`, and `list_all`.

### Read-Only

- `id` (String) The unique identifier for the data source operation.
- `items` (Attributes List) Forwarding profiles matching the query criteria. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `client_fallback_action` (String) The client fallback action.
- `description` (String) The forwarding profile description.
- `id` (String) The forwarding profile ID.
- `is_custom_profile` (Boolean) Whether this is a custom forwarding profile.
- `last_modified_date_time` (String) The last modified timestamp.
- `name` (String) The forwarding profile name.
- `policies` (Attributes List) Forwarding policy links associated with the profile. The policy link ID and policy ID are distinct Graph identifiers. (see [below for nested schema](#nestedatt--items--policies))
- `priority` (Number) The forwarding profile priority.
- `service_principal_app_id` (String) The associated service principal application ID.
- `service_principal_id` (String) The associated service principal object ID.
- `state` (String) The forwarding profile state.
- `traffic_forwarding_type` (String) The traffic forwarding type, such as `internet`, `m365`, or `private`.
- `version` (String) The forwarding profile version.

<a id="nestedatt--items--policies"></a>
### Nested Schema for `items.policies`

Read-Only:

- `policy_description` (String) The linked forwarding policy description.
- `policy_id` (String) The linked forwarding policy ID.
- `policy_link_id` (String) The forwarding policy link ID.
- `policy_name` (String) The linked forwarding policy name.
- `policy_version` (String) The linked forwarding policy version.
- `priority` (Number) The forwarding policy link priority.
- `private_access_app_id` (String) The linked private access app ID when returned by Graph.
- `state` (String) The forwarding policy link state.
- `traffic_forwarding_type` (String) The linked forwarding policy traffic forwarding type.
- `version` (String) The forwarding policy link version.
