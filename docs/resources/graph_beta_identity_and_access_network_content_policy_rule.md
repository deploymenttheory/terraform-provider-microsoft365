---
page_title: "microsoft365_graph_beta_identity_and_access_network_content_policy_rule Resource - terraform-provider-microsoft365"
subcategory: "Identity and Access"
description: |-
  Manages rules within Microsoft Entra Global Secure Access content policies using the Microsoft Graph beta /networkaccess/filePolicies/{contentPolicyId}/policyRules endpoint.
---

# microsoft365_graph_beta_identity_and_access_network_content_policy_rule (Resource)

Manages rules within Microsoft Entra Global Secure Access content policies using the Microsoft Graph beta `/networkaccess/filePolicies/{contentPolicyId}/policyRules` endpoint.

This resource manages rules within a Microsoft Entra Global Secure Access content policy.

## Microsoft Documentation

Microsoft Graph beta metadata and Microsoft Learn do not currently expose the portal-backed `fileRule` type used by this resource. The implementation is based on Microsoft Entra admin center traffic and live Microsoft Graph beta verification.

## Microsoft Graph API

This resource uses:

```http
POST   /beta/networkaccess/filePolicies/{contentPolicyId}/policyRules
GET    /beta/networkaccess/filePolicies/{contentPolicyId}/policyRules/{ruleId}
PATCH  /beta/networkaccess/filePolicies/{contentPolicyId}/policyRules/{ruleId}
DELETE /beta/networkaccess/filePolicies/{contentPolicyId}/policyRules/{ruleId}
```

The Terraform destination types map to the following Graph discriminator values:

- `web_category`: `#microsoft.graph.networkaccess.filePolicyWebCategoryDestination`
- `fqdn`: `#microsoft.graph.networkaccess.filePolicyFqdnDestination`
- `url`: `#microsoft.graph.networkaccess.filePolicyUrlDestination`

## Microsoft Graph API Permissions

The following client `application` permissions are needed in order to use this resource:

**Required:**
- `NetworkAccess.Read.All`
- `NetworkAccess.ReadWrite.All`

**Optional:**
- `None` `[N/A]`

## Example Usage

```terraform
resource "microsoft365_graph_beta_identity_and_access_network_content_policy" "example" {
  name           = "Content Policy"
  description    = "Global Secure Access content policy managed by Terraform"
  default_action = "allow"
}

resource "microsoft365_graph_beta_identity_and_access_network_content_policy_rule" "example" {
  content_policy_id  = microsoft365_graph_beta_identity_and_access_network_content_policy.example.id
  name               = "Inspect uploaded and downloaded files"
  description        = "Scan matching files with Microsoft Purview"
  action             = "scanPurview"
  priority           = 101
  status             = "enabled"
  activities         = ["download", "upload"]
  content_types      = ["text/csv", "application/pdf"]
  text_content_types = ["json", "plain", "html", "xml"]

  destinations = [
    {
      type   = "web_category"
      values = ["AlcoholAndTobacco"]
    },
    {
      type   = "fqdn"
      values = ["example.com", "*.example.com"]
    },
    {
      type   = "url"
      values = ["https://example.com/path"]
    }
  ]

  session_types = ["user", "agent"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `action` (String) The action applied to matching files. Possible values are `allow`, `block`, and `scanPurview`.
- `activities` (Set of String) File activities matched by the rule. Possible values are `download` and `upload`.
- `content_policy_id` (String) The content policy ID that owns this rule.
- `destinations` (Attributes List) Destination groups matched by the rule. (see [below for nested schema](#nestedatt--destinations))
- `name` (String) The rule name.
- `priority` (Number) The rule priority. Lower numbers are evaluated first.
- `session_types` (Set of String) Session types matched by the rule. Possible values are `user` and `agent`.
- `status` (String) The rule status. Possible values are `enabled` and `disabled`.

### Optional

- `content_types` (Set of String) MIME content types matched by the rule.
- `description` (String) The rule description. Defaults to an empty string.
- `text_content_types` (Set of String) Text content types matched by the rule. Possible values are `json`, `plain`, `html`, and `xml`.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `id` (String) The content policy rule ID.

<a id="nestedatt--destinations"></a>
### Nested Schema for `destinations`

Required:

- `type` (String) Destination type. Possible values are `web_category`, `fqdn`, and `url`.
- `values` (Set of String) Destination values for this group.


<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
#!/bin/bash
# Import using the format: {content_policy_id}/{rule_id}
terraform import microsoft365_graph_beta_identity_and_access_network_content_policy_rule.example 00000000-0000-0000-0000-000000000000/11111111-1111-1111-1111-111111111111
```
