---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "microsoft365_graph_beta_identity_and_access_network_prompt_policy Resource - Microsoft 365"
subcategory: ""
description: |-
  Manages Microsoft Entra Global Secure Access prompt policies using the portal-backed Microsoft Graph beta /networkaccess/promptPolicies endpoint. Rules are managed separately with microsoft365_graph_beta_identity_and_access_network_prompt_policy_rule. Deleting a policy also deletes all its rules, including rules outside Terraform. Creating a policy does not apply it to traffic; use a separate security profile policy link. See prompt injection protection https://learn.microsoft.com/en-us/entra/global-secure-access/how-to-ai-prompt-injection-protection.
---

# microsoft365_graph_beta_identity_and_access_network_prompt_policy (Resource)

Manages Microsoft Entra Global Secure Access prompt policies using the portal-backed Microsoft Graph beta `/networkaccess/promptPolicies` endpoint. Rules are managed separately with `microsoft365_graph_beta_identity_and_access_network_prompt_policy_rule`. Deleting a policy also deletes all its rules, including rules outside Terraform. Creating a policy does not apply it to traffic; use a separate security profile policy link. See [prompt injection protection](https://learn.microsoft.com/en-us/entra/global-secure-access/how-to-ai-prompt-injection-protection).

## Example Usage

```terraform
# Creating policies and rules does not assign them to traffic.
resource "microsoft365_graph_beta_identity_and_access_network_prompt_policy" "example" {
  name        = "Example prompt policy"
  description = "Prompt protection for AI applications"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The nonempty name of the prompt policy.

### Optional

- `default_action` (String) The action when no rule matches. Only `allow` is supported. Defaults to `allow`.
- `description` (String) The policy description. Omission clears the description to null; an empty string is preserved.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `id` (String) The unique identifier for the prompt policy.
- `last_modified_date_time` (String) The last modification timestamp. Changes to child rules can also change this value.
- `version` (String) The API-assigned policy version. Read-only.

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# {policy_id}
terraform import microsoft365_graph_beta_identity_and_access_network_prompt_policy.example '{policy_id}'
```
