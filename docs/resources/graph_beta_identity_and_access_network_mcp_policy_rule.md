---
page_title: "microsoft365_graph_beta_identity_and_access_network_mcp_policy_rule Resource - terraform-provider-microsoft365"
subcategory: "Identity and Access"
description: |-
  Manages independent rules under Microsoft Entra Global Secure Access MCP policies using the portal-backed Graph beta endpoint. Changing mcp_policy_id replaces the rule. Names and priorities must be unique within the parent. The API accepts rules without conditions. Creating a rule does not assign its policy to traffic. Condition persistence has been verified; traffic evaluation, AND/OR behavior and pattern semantics have not been verified. All CRUD operation timeouts default to 3 minutes.
---

# microsoft365_graph_beta_identity_and_access_network_mcp_policy_rule (Resource)

Manages independent rules under Microsoft Entra Global Secure Access MCP policies using the portal-backed Graph beta endpoint. Changing mcp_policy_id replaces the rule. Names and priorities must be unique within the parent. The API accepts rules without conditions. Creating a rule does not assign its policy to traffic. Condition persistence has been verified; traffic evaluation, AND/OR behavior and pattern semantics have not been verified. All CRUD operation timeouts default to 3 minutes.

## API Support

This resource uses the portal-backed Microsoft Graph beta MCP policy API, which is not represented in the generated Graph SDK used by this provider. Endpoint availability depends on the tenant's Global Secure Access feature and licensing. API persistence was tested with disposable, unassigned policies; traffic evaluation was not tested.

## Microsoft Graph API Permissions

For application authentication, configure `NetworkAccess.ReadWrite.All` with administrator consent for CRUD operations. Read-only operations require `NetworkAccess.Read.All` or `NetworkAccess.ReadWrite.All`. Delegated authentication also depends on the signed-in user's role and the client application's granted scopes.

## Example Usage

```terraform
resource "microsoft365_graph_beta_identity_and_access_network_mcp_policy" "test" {
  name           = "tf-api-probe-policy"
  description    = "Initial description"
  default_action = "allow"
}

resource "microsoft365_graph_beta_identity_and_access_network_mcp_policy_rule" "test" {
  mcp_policy_id       = microsoft365_graph_beta_identity_and_access_network_mcp_policy.test.id
  name                = "tf-api-probe-rule"
  description         = "Initial description"
  action              = "allow"
  priority            = 1000
  enabled             = true
  matching_conditions = { tool_matching = { names = { values = ["tf_probe_tool"], match_type = "exactMatch" }, methods = "call" } }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `action` (String) The action applied to matching destinations: `allow` or `block`.
- `enabled` (Boolean) Whether the rule is enabled. Must be explicitly configured.
- `mcp_policy_id` (String) The MCP policy ID that owns this rule.
- `name` (String) The nonempty rule name.
- `priority` (Number) The rule priority, from 100 through 2147483647. Must be unique within the parent policy. Use a spare priority when swapping rules.

### Optional

- `description` (String) The rule description. Omission clears it to null; an empty string is preserved.
- `matching_conditions` (Attributes) MCP destination conditions. Omission clears all conditions. Arrays preserve input order; the provider does not normalize URLs or infer traffic matching semantics. (see [below for nested schema](#nestedatt--matching_conditions))
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `id` (String) The MCP policy rule ID.
- `status` (String) The raw API rule status. Normally `enabled` or `disabled`; unknown values are diagnosed rather than converted to enabled.

<a id="nestedatt--matching_conditions"></a>
### Nested Schema for `matching_conditions`

Optional:

- `insecure_connection` (String) Whether this property is required or excluded by the condition. Omission removes this constraint.
- `missing_prm` (String) Whether this property is required or excluded by the condition. Omission removes this constraint.
- `prompt_matching` (Attributes) Primitive conditions applied to the rule. Multiple primitive types may be stored by the API; their combined traffic evaluation is not verified. (see [below for nested schema](#nestedatt--matching_conditions--prompt_matching))
- `protocol_versions` (Attributes) String matching condition. Values are preserved without URL or case normalization. (see [below for nested schema](#nestedatt--matching_conditions--protocol_versions))
- `resource_matching` (Attributes) Primitive conditions applied to the rule. Multiple primitive types may be stored by the API; their combined traffic evaluation is not verified. (see [below for nested schema](#nestedatt--matching_conditions--resource_matching))
- `server_urls` (Attributes) String matching condition. Values are preserved without URL or case normalization. (see [below for nested schema](#nestedatt--matching_conditions--server_urls))
- `tool_matching` (Attributes) Primitive conditions applied to the rule. Multiple primitive types may be stored by the API; their combined traffic evaluation is not verified. (see [below for nested schema](#nestedatt--matching_conditions--tool_matching))

<a id="nestedatt--matching_conditions--prompt_matching"></a>
### Nested Schema for `matching_conditions.prompt_matching`

Optional:

- `methods` (String) API method flags, for example call or list,call for tools, read for resources, or get for prompts. Values are sent unchanged; unsupported combinations return API diagnostics.
- `names` (Attributes) Primitive name matching. (see [below for nested schema](#nestedatt--matching_conditions--prompt_matching--names))

<a id="nestedatt--matching_conditions--prompt_matching--names"></a>
### Nested Schema for `matching_conditions.prompt_matching.names`

Required:

- `match_type` (String) Match operator: exactMatch, contains, notExactMatch, or notContains. Must be explicitly configured.
- `values` (List of String) Values in API order. Empty arrays are preserved and are distinct from an omitted condition. Pattern evaluation is performed by the service; regex or wildcard behavior is not guaranteed by this resource.



<a id="nestedatt--matching_conditions--protocol_versions"></a>
### Nested Schema for `matching_conditions.protocol_versions`

Required:

- `match_type` (String) Match operator: exactMatch, contains, notExactMatch, or notContains. Must be explicitly configured.
- `values` (List of String) Values in API order. Empty arrays are preserved and are distinct from an omitted condition. Pattern evaluation is performed by the service; regex or wildcard behavior is not guaranteed by this resource.


<a id="nestedatt--matching_conditions--resource_matching"></a>
### Nested Schema for `matching_conditions.resource_matching`

Optional:

- `methods` (String) API method flags, for example call or list,call for tools, read for resources, or get for prompts. Values are sent unchanged; unsupported combinations return API diagnostics.
- `names` (Attributes) Primitive name matching. (see [below for nested schema](#nestedatt--matching_conditions--resource_matching--names))

<a id="nestedatt--matching_conditions--resource_matching--names"></a>
### Nested Schema for `matching_conditions.resource_matching.names`

Required:

- `match_type` (String) Match operator: exactMatch, contains, notExactMatch, or notContains. Must be explicitly configured.
- `values` (List of String) Values in API order. Empty arrays are preserved and are distinct from an omitted condition. Pattern evaluation is performed by the service; regex or wildcard behavior is not guaranteed by this resource.



<a id="nestedatt--matching_conditions--server_urls"></a>
### Nested Schema for `matching_conditions.server_urls`

Required:

- `match_type` (String) Match operator: exactMatch, contains, notExactMatch, or notContains. Must be explicitly configured.
- `values` (List of String) Values in API order. Empty arrays are preserved and are distinct from an omitted condition. Pattern evaluation is performed by the service; regex or wildcard behavior is not guaranteed by this resource.


<a id="nestedatt--matching_conditions--tool_matching"></a>
### Nested Schema for `matching_conditions.tool_matching`

Optional:

- `methods` (String) API method flags, for example call or list,call for tools, read for resources, or get for prompts. Values are sent unchanged; unsupported combinations return API diagnostics.
- `names` (Attributes) Primitive name matching. (see [below for nested schema](#nestedatt--matching_conditions--tool_matching--names))

<a id="nestedatt--matching_conditions--tool_matching--names"></a>
### Nested Schema for `matching_conditions.tool_matching.names`

Required:

- `match_type` (String) Match operator: exactMatch, contains, notExactMatch, or notContains. Must be explicitly configured.
- `values` (List of String) Values in API order. Empty arrays are preserved and are distinct from an omitted condition. Pattern evaluation is performed by the service; regex or wildcard behavior is not guaranteed by this resource.




<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

```shell
# Replace the example UUIDs with your existing policy and rule IDs.
terraform import microsoft365_graph_beta_identity_and_access_network_mcp_policy_rule.test 11111111-1111-1111-1111-111111111111/22222222-2222-2222-2222-222222222222
```
