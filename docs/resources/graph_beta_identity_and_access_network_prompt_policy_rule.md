---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "microsoft365_graph_beta_identity_and_access_network_prompt_policy_rule Resource - Microsoft 365"
subcategory: ""
description: |-
  Manages independent rules within Microsoft Entra Global Secure Access prompt policies using the Microsoft Graph beta /networkaccess/promptPolicies/{promptPolicyId}/policyRules endpoint. The parent policy is managed separately. Priority must be unique within a policy; use a spare priority when swapping rules. Creating a rule does not apply the policy to traffic.
---

# microsoft365_graph_beta_identity_and_access_network_prompt_policy_rule (Resource)

Manages independent rules within Microsoft Entra Global Secure Access prompt policies using the Microsoft Graph beta `/networkaccess/promptPolicies/{promptPolicyId}/policyRules` endpoint. The parent policy is managed separately. Priority must be unique within a policy; use a spare priority when swapping rules. Creating a rule does not apply the policy to traffic.

## Example Usage

```terraform
# Creating policies and rules does not assign them to traffic.
resource "microsoft365_graph_beta_identity_and_access_network_prompt_policy" "example" {
  name        = "Example prompt policy"
  description = "Prompt protection for AI applications"
}

resource "microsoft365_graph_beta_identity_and_access_network_prompt_policy_rule" "example" {
  prompt_policy_id = microsoft365_graph_beta_identity_and_access_network_prompt_policy.example.id
  name             = "Block malicious prompts"
  priority         = 1000
  enabled          = true
  action           = "block"
  prompt_logging   = "onBlock"
  conversation_schemes = [
    {
      type        = "predefined"
      scheme_name = "chatGpt"
    },
    {
      type      = "custom"
      url       = "https://ai.example.com/chat"
      json_path = "$.prompt"
    }
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `action` (String) The action applied to matching prompts: `allow` or `block`. Must be explicitly configured.
- `conversation_schemes` (Attributes List) Conversation schemes matched by the rule. The entire list is managed together. An empty list is accepted by the API; its effect on traffic has not been verified. (see [below for nested schema](#nestedatt--conversation_schemes))
- `enabled` (Boolean) Whether the rule is enabled. Must be explicitly configured.
- `name` (String) The nonempty rule name.
- `priority` (Number) The rule priority, from 100 through 2147483647. Lower numbers are evaluated first. Must be unique within the parent policy.
- `prompt_policy_id` (String) The prompt policy ID that owns this rule.

### Optional

- `description` (String) The rule description. Omission clears it to null; an empty string is preserved.
- `prompt_logging` (String) When to log prompts: `never`, `always`, or `onBlock`. Defaults to `never`.
- `scan_result` (String) The scan result to match. Defaults to `maliciousPromptDetected`, the currently supported value.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `id` (String) The prompt policy rule ID.
- `status` (String) The raw API rule status. Normally `enabled` or `disabled`.

<a id="nestedatt--conversation_schemes"></a>
### Nested Schema for `conversation_schemes`

Required:

- `type` (String) Scheme type: `custom` or `predefined`.

Optional:

- `json_path` (String) JSONPath locating the prompt in a custom request. Only valid for custom schemes. When omitted the API uses an empty string; an explicitly configured empty string is preserved.
- `scheme_name` (String) The predefined scheme name. Required for predefined schemes and forbidden for custom schemes.
- `url` (String) The HTTP or HTTPS endpoint URL for a custom scheme. Required for custom schemes and forbidden for predefined schemes. Include a path, such as `/chat` or `/`.


<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# {prompt_policy_id}/{rule_id}
terraform import microsoft365_graph_beta_identity_and_access_network_prompt_policy_rule.example '{prompt_policy_id}/{rule_id}'
```
