---
page_title: "microsoft365_graph_beta_identity_and_access_network_tls_inspection_policy_rule Resource - terraform-provider-microsoft365"
subcategory: "Identity and Access"
description: |-
  Manages rules within Microsoft Entra Global Secure Access TLS inspection policies using the Microsoft Graph beta /networkaccess/tlsInspectionPolicies/{tlsInspectionPolicyId}/policyRules endpoint. The parent policy is managed separately. Priority must be unique within the policy; use a spare priority when swapping rules. The automatically created system rule (priority 50) cannot be managed by this resource. The recommended bypass rule may be imported explicitly. This resource requires at least one nonempty destination group. Creating a rule does not apply the policy to traffic.
---

# microsoft365_graph_beta_identity_and_access_network_tls_inspection_policy_rule (Resource)

Manages rules within Microsoft Entra Global Secure Access TLS inspection policies using the Microsoft Graph beta `/networkaccess/tlsInspectionPolicies/{tlsInspectionPolicyId}/policyRules` endpoint. The parent policy is managed separately. Priority must be unique within the policy; use a spare priority when swapping rules. The automatically created system rule (priority 50) cannot be managed by this resource. The recommended bypass rule may be imported explicitly. This resource requires at least one nonempty destination group. Creating a rule does not apply the policy to traffic.

## Microsoft Documentation

- [TLS inspection](https://learn.microsoft.com/en-us/entra/global-secure-access/how-to-transport-layer-security)
- [Graph API resource](https://learn.microsoft.com/en-us/graph/api/resources/networkaccess-tlsinspectionrule?view=graph-rest-beta)

## Microsoft Graph API Permissions

Reading requires `NetworkAccess.Read.All`; writing requires `NetworkAccess.ReadWrite.All`. Microsoft Graph documents both application and delegated permissions. Delegated callers also require an appropriate administrator role such as Global Secure Access Administrator or Security Administrator.

## Behavior Notes

- The policy and each rule have separate Terraform lifecycles. Set `tls_inspection_policy_id` to the parent resource ID.
- `enabled` is required and maps to the rule's API status; `status` reports that raw value. Unknown API statuses produce a diagnostic rather than being interpreted as disabled.
- Updates to name, description, action, priority, enabled and destinations preserve the rule ID. Changing the parent policy replaces the rule.
- Priorities must be unique per policy. The recommended bypass rule initially occupies priority 65000. For priority swaps, move one rule to an unused priority in a separate apply first.
- The system rule at priority 50 is outside this resource's supported scope. The recommended bypass rule can be imported explicitly using its parent and rule IDs.
- At least one destination group and one nonempty value per group are required by the provider. Group order is preserved; values are an unordered set.
- Omitting `description` clears it to null; an explicit empty string remains an empty string.

All CRUD operations default to a timeout of **3 minutes**. Configure `timeouts` to override them.

## Example Usage

```terraform
resource "microsoft365_graph_beta_identity_and_access_network_tls_inspection_policy" "example" {
  name           = "Example TLS inspection policy"
  description    = "Initial description"
  default_action = "inspect"
}
resource "microsoft365_graph_beta_identity_and_access_network_tls_inspection_policy_rule" "example" {
  tls_inspection_policy_id = microsoft365_graph_beta_identity_and_access_network_tls_inspection_policy.example.id
  name                     = "Inspect selected destinations"
  description              = "Initial description"
  action                   = "inspect"
  priority                 = 1000
  enabled                  = true
  destinations             = [{ type = "fqdn", values = ["example.com", "*.example.org"] }]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `action` (String) The action applied to matching destinations: `inspect` or `bypass`.
- `destinations` (Attributes List) Destination groups matched by the rule. (see [below for nested schema](#nestedatt--destinations))
- `enabled` (Boolean) Whether the rule is enabled. Must be explicitly configured.
- `name` (String) The nonempty rule name.
- `priority` (Number) The rule priority, from 100 through 2147483647. Lower numbers are evaluated first. Must be unique within the parent policy; 65000 is initially used by the recommended bypass rule.
- `tls_inspection_policy_id` (String) The TLS inspection policy ID that owns this rule.

### Optional

- `description` (String) The rule description. Omission clears it to null; an empty string is preserved.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `id` (String) The TLS inspection policy rule ID.
- `status` (String) The raw API rule status. Normally `enabled` or `disabled`; this is not a certificate lifecycle status.

<a id="nestedatt--destinations"></a>
### Nested Schema for `destinations`

Required:

- `type` (String) Destination type. Possible values are `web_category` and `fqdn`.
- `values` (Set of String) Destination values for this group.


<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

```shell
# Import with the parent policy UUID and rule UUID separated by a slash.
terraform import microsoft365_graph_beta_identity_and_access_network_tls_inspection_policy_rule.example 00000000-0000-0000-0000-000000000001/00000000-0000-0000-0000-000000000002
```
